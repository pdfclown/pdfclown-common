#
# On a daily basis, publishes SNAPSHOT artifacts to Maven Central Portal Snapshots repository.
#
name: Nightly Build

on:
  schedule:
    - cron: '0 4 * * *'  # Once a day, at 4:00 UTC
  workflow_dispatch:

jobs:
  build:
    uses: pdfclown/.github/.github/workflows/build-nightly.yml@13af571d3e068b7616aa250717425665c236f90e
    permissions:
      contents: read
    secrets: inherit
