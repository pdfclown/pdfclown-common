#
# On a daily basis, publishes SNAPSHOT artifacts to Maven Central Portal Snapshots repository.
#
name: Nightly Build

on:
  schedule:
    - cron: '0 4 * * *'  # Once a day, at 4:00 UTC
  workflow_dispatch:

jobs:
  build:
    uses: pdfclown/.github/.github/workflows/build-nightly.yml@edf09a273a7e408850468143be99c6009cd0461e
    permissions:
      contents: read
    secrets: inherit
