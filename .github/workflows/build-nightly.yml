#
# On a daily basis, publishes SNAPSHOT artifacts to Maven Central Portal Snapshots repository.
#
name: Nightly Build

on:
  schedule:
    - cron: '0 4 * * *'  # Once a day, at 4:00 UTC
  workflow_dispatch:

jobs:
  build:
    uses: pdfclown/.github/.github/workflows/build-nightly.yml@0443b2f0dffb509eace4cc5f8c592156df65007a
    permissions:
      contents: read
    secrets: inherit
