#
# On pull request events, verifies the PR conforms to project conventions.
#
name: Pull Request

on:
  pull_request_target:
    types:
      - opened
      - reopened
      - edited

jobs:
  main:
    uses: pdfclown/.github/.github/workflows/pull-request.yml@edf09a273a7e408850468143be99c6009cd0461e
    permissions:
      pull-requests: read
    secrets: inherit
