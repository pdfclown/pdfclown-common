#
# On pull request events, verifies the PR conforms to project conventions.
#
name: Pull Request

on:
  pull_request_target:
    types:
      - opened
      - reopened
      - edited

jobs:
  main:
    uses: pdfclown/.github/.github/workflows/pull-request.yml@0443b2f0dffb509eace4cc5f8c592156df65007a
    permissions:
      pull-requests: read
    secrets: inherit
